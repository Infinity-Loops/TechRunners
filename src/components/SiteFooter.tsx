export function SiteFooter() {
  return (
    <footer className="mt-auto border-t border-line/70 bg-ink/70">
      <div className="mx-auto flex max-w-6xl flex-col items-center justify-between gap-2 px-4 py-6 text-center text-muted sm:flex-row sm:text-left">
        <p className="font-pixel text-[10px] leading-relaxed">
          © TECHRUNNERS · PLAYTEST BUILD
        </p>
        <p className="text-sm">
          Be a runner. Stay a runner. Never stop running.
        </p>
      </div>
    </footer>
  );
}
